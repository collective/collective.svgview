Introduction
============

This product provides a view for SVG files. SVG files are treated by plone
out of the box as xml files so the XML body gets displayed instead of the
SVG image. collective svgview adds a view to the ATFile which displays
SVG images as embedded images.
